<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_map"
        android:icon="@drawable/ic_setting"
        android:title="Select Vehicle" />

        <item android:id="@+id/nav_maint"
            android:icon="@drawable/ic_maintenancelist"
            android:title="Maintenance List" />

        <item android:id="@+id/nav_bike"
            android:icon="@drawable/ic_map"
            android:title="Nearest Garage" />
    </group>

    <item android:title="Information">
        <menu>
            <item android:id="@+id/nav_email"
                android:icon="@drawable/ic_email"
                android:title="Send email" />

            <item android:id="@+id/nav_call"
                android:icon="@drawable/ic_call"
                android:title="Call Us" />

            <item android:id="@+id/nav_facebook"
                android:icon="@drawable/ic_facebook"
                android:title="Facebook Fan Page" />
        </menu>

    </item>

</menu>